# Design Spec: Dynamic Opname Messages`n`n## Goal`nUpdate the static "Data Opname Sudah Terisi" message in the Gantt Chart to reflect the actual status of the submitted opname (Pending, Disetujui, Ditolak).`n`n## Proposed Text`nBased on User preference (Option A):`n- **Pending**: "Menunggu Review. Anda sudah mengajukan opname untuk item ini dan sedang menunggu persetujuan tim Support."`n- **Disetujui**: "Opname Disetujui. Pengajuan opname untuk item ini telah disetujui oleh tim Support."`n- **Ditolak**: "Opname Ditolak. Pengajuan opname ditolak. Silakan lakukan revisi melalui Menu Opname."`n`n## Implementation Details`n- File: `app/gantt/page.tsx``n- Component: Update the static alert box rendering inside `MemoPengawasanModal` where `currentStatus === 'Selesai' && isWorkItemBlockedByOpname(...)` is checked.`n- Logic: Use `opnameForStatus.status` to determine the exact message, icon, and colors (e.g., Yellow for pending, Green for approved, Red for rejected).
